import { StyleSheet, Text, View, Image } from 'react-native';
type ParamsType = {
}
export const Feedback = ({}:ParamsType) => {
    return (
        <View>
            <Text>fedback</Text>
        </View>
    );
}
const styles = StyleSheet.create({

});